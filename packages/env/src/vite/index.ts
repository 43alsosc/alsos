/**
 * Vite / TanStack: use `createEnv` from `@t3-oss/env-core` with `clientPrefix: "VITE_"` and schemas from `@alsos/env/shared`.
 */
export { createEnv } from '@t3-oss/env-core';
export * from '../shared/index.js';
